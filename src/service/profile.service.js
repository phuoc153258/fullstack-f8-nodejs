const Course = require('../models/Course');
const User = require('../models/User');
const UserCourse = require('../models/UserCourse');

const getInfoProfileService = async (slug) => {

}

module.exports = {
    getInfoProfileService
}